{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "(cd ../../packages/contracts && pnpm run build) && (cd ../../packages/database && pnpm run db:generate) && pnpm run build",
  "framework": "nextjs",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [{ "key": "X-Robots-Tag", "value": "noindex, nofollow" }]
    }
  ]
}
