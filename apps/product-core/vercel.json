{
  "installCommand": "pnpm install --frozen-lockfile",
  "ignoreCommand": "bash -c 'if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"main-dev\" ]; then exit 1; else exit 0; fi'",
  "buildCommand": "(cd ../../packages/contracts && pnpm run build) && (cd ../../packages/validation && pnpm run build) && (cd ../../packages/domain && pnpm run build) && (cd ../../packages/database && pnpm run build && pnpm run db:generate) && pnpm run build",
  "framework": "nextjs"
}
